import { type Transaction } from '@/types/types'

interface Props {
  transaction: Transaction
}

const EditTransaction = ({ transaction }: Props): JSX.Element => {
  return (
    <div>EditTransaction</div>
  )
}

export default EditTransaction
